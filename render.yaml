services:
  - type: web
    name: guardedpy-demo
    runtime: python
    plan: free
    buildCommand: pip install .
    startCommand: uvicorn guardedpy.demo:create_demo_app --factory --host 0.0.0.0 --port $PORT
    autoDeploy: false
